before:
  hooks:
    - go mod download
builds:
  - binary: helm-clean-values
    main: ./cmd/helm-clean-values
    env:
      - CGO_ENABLED=0
    goos:
      - darwin
      - linux
    goarch:
      - amd64
      - arm64
      - "386"
